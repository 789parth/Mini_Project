<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
<!-- State when clicked/pressed (Grey) -->
<item android:state_pressed="true">
    <shape android:shape="rectangle">
        <solid android:color="@color/white" /> <!-- White Background -->
        <stroke android:width="3dp" android:color="#D3D3D3" /> <!-- Grey Border -->
        <corners android:radius="8dp" />
    </shape>
</item>

<!-- Normal State (Teal) -->
<item>
    <shape android:shape="rectangle">
        <solid android:color="@color/white" /> <!-- White Background -->
        <stroke android:width="3dp" android:color="@color/btn_color" /> <!-- Teal Border -->
        <corners android:radius="8dp" />
    </shape>
</item>
</selector>
